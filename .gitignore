# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
# Alternate dist dir used when a build has to run without touching the dev
# server's .next — same build output, same reason to stay out of git.
/.next-build/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# vercel
.vercel

# editor
.idea
.vscode

# Admin-uploaded images (city photos, etc.)
/public/uploads/
